
task enablePCSCD(description: 'Enable PCSCD service.') << {
  remote {
    // TODO: sudo rm -rf /etc/init.d/device
    // TODO: sudo apt-get -yq install python-pyscard pcscd pcsc-tools
    // TODO: sudo chmod u+s /usr/sbin/pcscd
    // TODO: sudo nano /lib/systemd/system/pcscd.service
    // TODO:   ExecStart=/usr/sbin/pcscd --foreground --auto-exit --debug
    // TODO:   Restart=always
    // TODO: sudo nano /etc/crontab   
    // TODO:   *  *    * * *   root    /etc/init.d/pcscd start
  }
}
